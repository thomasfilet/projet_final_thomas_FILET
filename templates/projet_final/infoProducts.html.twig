{% extends 'base.html.twig' %}

{% block body %}
<section class="bg-black w-full">
    <section>
        {{ include('projet_final/HeaderUser.html.twig') }}
    </section>

    <div class="mx-36 mt-8 w-20">
        <a class="text-white" href="{{path('app_homepageUser')}}">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-12 h-12">
               <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 12h-15m0 0l6.75 6.75M4.5 12l6.75-6.75" />
           </svg>
       </a>
    </div>

    <section class="flex justify-center">
        <div class="flex">
            <div class=" mr-20 pt-32 ml-10 w-96">
                <h1 class="text-5xl font-bold pt-2"> {{montre.getModel}}</h1>
                <h2 class="titre_color text-4xl pb-2">{{montre.getMake}}</h2>
                <h3 class="text-2xl pb-2 mt-4">{{montre.getPrice}} €</h3>
                <h3 class="text-lg pb-2 mt-4">{{montre.getDescription}}</h3>
            </div>
        </div>
        <img class="all_infoProduit " src="{{montre.getImage}}" alt="">   
    </section>

    <div class="flex justify-center mt-20">
        <div class="w-4/5 text-center bar"></div>
    </div>

    <h2 class="text-xl mt-16 ml-36">If you are looking for a brand watch : {{montre.getMake}}</h2>

    <section class='mt-4 mb-10 flex justify-center w-full'>
        <div class="container">
            {% for montre in montre_object %}
                <a href="{{path('app_montre_id',{id:montre.getId})}}" class="item mx-8 relative">
                    <img class="imgage" src='{{montre.getImage}}'></img>
                    <div class="w-full mt-2 text-center">
                        <h2 class="text-2xl font-bold text-white pt-2 uppercase"> {{montre.getModel}}</h1>
                        <h3 class="titre_color text-lg pb-2">{{montre.getMake}}</h1>
                    </div>
                </a>
            {% endfor %}
        </div>
    </section>
</section>
{% endblock %}